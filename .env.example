# Copy to .env and fill values. Restart dev server after changes.
# Vite requires env vars to start with VITE_
VITE_BIO_REF_API_URL=https://api.example.com/bioref
VITE_BIO_REF_API_KEY=your_api_key_here
# Optional: customize header name if provider uses something else (e.g., Authorization)
VITE_BIO_REF_AUTH_HEADER=x-api-key
